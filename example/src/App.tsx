import * as React from 'react';
import {Component} from 'react'

import ExampleComponent from 'react-jsonschema-form-ts';

export default class App extends Component {
  render() {
    return (
      <div>
        <ExampleComponent text='Modern React component module'/>
      </div>
    );
  }
}
